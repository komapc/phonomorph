name: Auto-Fill Transformations

on:
  schedule:
    - cron: '*/10 * * * *'   # every 10 minutes
  workflow_dispatch:
    inputs:
      batch_size:
        description: "Candidates to process per run (default: 30)"
        required: false
        default: "30"
        type: string

concurrency:
  group: auto-fill
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  auto-fill:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: eu-central-1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install Node dependencies
        run: npm ci

      - name: Install Python dependencies
        run: pip install google-genai boto3

      - name: Run auto-fill script
        run: python scripts/auto-fill.py
        env:
          BATCH_SIZE: ${{ inputs.batch_size || '30' }}

      - name: Count filled transformations
        id: count
        if: always()
        run: |
          # If the script crashed before writing the summary, salvage any filled
          # files left in the working tree by counting them directly.
          if [ -f /tmp/auto-fill-summary.json ]; then
            N=$(python3 -c "import json; d=json.load(open('/tmp/auto-fill-summary.json')); print(len(d['filled']))")
          else
            N=$(git status --porcelain public/data/transformations/ | grep -c '^??' || true)
          fi
          # Defensive rebuild in case the script crashed before its own rebuild
          # ran. Must succeed before we expose `filled` to downstream steps —
          # we don't want to push a broken index.
          if [ "$N" != "0" ]; then
            npm run rebuild-index
          fi
          echo "filled=$N" >> "$GITHUB_OUTPUT"

      - name: Commit and push branch
        if: always() && steps.count.outputs.filled != '0'
        id: push
        run: |
          BRANCH="auto-fill/$(date +%Y-%m-%d-%H%M)"
          git checkout -b "$BRANCH"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/data/transformations/ public/data/index.json public/data/shards/
          N=${{ steps.count.outputs.filled }}
          git commit -m "docs: auto-fill $N transformations via Gemini 2.0 Flash"
          git push origin "$BRANCH"
          echo "branch=$BRANCH" >> "$GITHUB_OUTPUT"

      - name: Create pull request
        if: always() && steps.count.outputs.filled != '0' && steps.push.outputs.branch != ''
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          N=${{ steps.count.outputs.filled }}
          # Fallback body if the script crashed before writing /tmp/pr-body.md.
          if [ ! -f /tmp/pr-body.md ]; then
            echo "Salvaged $N transformation(s) from a crashed auto-fill run. Review carefully." > /tmp/pr-body.md
          fi
          gh pr create \
            --title "auto: fill $N phonetic transformations (Gemini 2.0 Flash)" \
            --body-file /tmp/pr-body.md \
            --head "${{ steps.push.outputs.branch }}" \
            --base master

      - name: Summary
        if: always()
        run: |
          if [ -f /tmp/auto-fill-summary.json ]; then
            echo "### Auto-fill summary" >> "$GITHUB_STEP_SUMMARY"
            python3 -c "
          import json
          d = json.load(open('/tmp/auto-fill-summary.json'))
          print(f'- Filled: {len(d[\"filled\"])}')
          print(f'- Skipped: {len(d[\"skipped\"])}')
          print(f'- Failed: {len(d[\"failed\"])}')
          " >> "$GITHUB_STEP_SUMMARY"
          else
            echo "Script did not produce a summary (no candidates found)." >> "$GITHUB_STEP_SUMMARY"
          fi
