{
  "fromId": "string",
  "toId": "string",
  "preamble": "string (popular science intro)",
  "phoneticEffects": "Allophony, [Other Effects]",
  "languageExamples": [
    {
      "language": "string",
      "languageFamily": "string (optional)",
      "examples": [
        { "from": "string (phoneme)", "to": "string (allophone)", "note": "Condition/Context" }
      ]
    }
  ],
  "certainty": 5,
  "commonality": 5,
  "sources": ["string (citations)"],
  "tags": ["Allophony", "string (Process)", "string (Family)"],
  "related": [
    { "fromId": "string", "toId": "string", "label": "string", "type": "chain | branch" }
  ]
}